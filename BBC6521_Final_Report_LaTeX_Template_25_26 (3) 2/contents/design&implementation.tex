% !TEX root = ../main.tex
\section*{Chapter 3: Design and Implementation}
\phantomsection \label{chap:design} \setcounter{section}{3} \setcounter{subsection}{0}
\addcontentsline{toc}{section}{\textit{Chapter 3: Design and Implementation}}

The design question for \texttt{linux-mcp} is a question of placement: which parts of the control plane should the gateway not be solely responsible for holding?
\texttt{mcpd} keeps everything that needs semantic flexibility; \texttt{kernel\_\allowbreak mcp} owns the state that must survive gateway failure and stay unreachable from unprivileged processes.



\subsection{Design Goals}
\label{sec:design-goals}

Chapter~\ref{chap:background} established that MCP-style control planes need enforcement beyond user-space self-policing. 
\texttt{linux-mcp} translates that conclusion into three design commitments.

\textbf{(1) Kernel-resident admission.}
Every tool invocation is admitted against kernel-resident state only: the registration of the requesting agent, the presence of the target tool in the registry, and the consistency of its semantic identity with the registered manifest.
The admission outcome does not depend on \texttt{mcpd}'s in-memory state at the moment of the decision.

\textbf{(2) No kernel-facing obligations for tool services.}
Tool authors write against an ordinary user-space IPC interface; the kernel-facing protocol is confined to \texttt{mcpd} and described in Section~\ref{sec:wire-protocols}.
This preserves compatibility with existing tool ecosystems and keeps incremental deployment practical.

\textbf{(3) Kernel-queryable observability.}
Admission, approval, and completion records are readable through kernel interfaces independently of \texttt{mcpd}'s liveness, so a daemon crash does not erase recent control-plane history.

What these commitments cover, and what they leave out (runtime semantic verification of tool behaviour, a general policy language, the broader attack surface of LLM-based systems), is fixed by the threat model of Chapter~\ref{chap:background}.



\subsection{System Architecture}
\label{sec:system-architecture}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/linux-mcp-overview.pdf}
\caption{Layered architecture of \texttt{linux-mcp}, showing the four runtime components and the wire protocols connecting them.}
\label{fig:linux-mcp-overview}
\end{figure}

The overall architecture of \texttt{linux-mcp}, shown in Figure~\ref{fig:linux-mcp-overview}, comprises four runtime components.

\texttt{llm-app} is the user-facing client.
It queries \texttt{mcpd} for the tool catalog, constructs invocation payloads from tool schemas, and submits requests.
The client has no direct interaction with the kernel module; all control-plane operations are mediated through \texttt{mcpd}.

\texttt{mcpd} is the user-space gateway and the semantic front-end of the control plane.
It is responsible for loading and validating tool manifests, computing manifest-derived semantic hashes, binding each session to the peer's \texttt{SO\_\allowbreak PEERCRED}-derived identity and registering that binding with the kernel, exposing catalog APIs (\texttt{list\_apps}, \texttt{list\_\allowbreak tools}), communicating with the kernel over Generic Netlink, and forwarding admitted requests to tool services over Unix domain sockets.
\texttt{mcpd} is the only component that sees both tool semantics and runtime endpoints, so it is the only one that needs changing when manifests or endpoints are reconfigured.

\texttt{kernel\_\allowbreak mcp} is the durable back-end of the control plane: a loadable kernel module implementing the \texttt{KERNEL\_\allowbreak MCP} Generic Netlink family.
It maintains the tool and agent registries, arbitrates invocation requests, manages approval ticket lifecycles, verifies the session binding hash on every request, pins a trust-on-first-use \texttt{binary\_\allowbreak hash} for each backend process, and records per-agent invocation history in a fixed-size \texttt{call\_\allowbreak log} ring buffer.
All kernel-held state is exposed read-only through \texttt{sysfs} under \texttt{/\allowbreak sys/\allowbreak kernel/\allowbreak mcp/}, which remains readable even when \texttt{mcpd} is not running.
The module does not parse manifests, execute tools, or implement a policy language; its only job is enforcing the admission-control invariants.

\texttt{tool-app} is the execution side: a collection of resident user-space services, each bound to a Unix domain socket.
When a request is admitted, \texttt{mcpd} forwards the payload to the appropriate tool service, which executes in an ordinary user-space environment and returns its result to \texttt{mcpd}, which then reports completion back to the kernel.
The directory \texttt{tool-app/\allowbreak manifests/} is the authoritative source of tool identity: \texttt{mcpd} reads manifests from here, hashes them, and registers the resulting digests with the kernel.


To clarify the separation of responsibilities across layers, Table~\ref{tab:architecture_layers} summarises each component together with its intended scope and explicitly excluded functionality.

\begin{table}[htbp]
\centering
\small
\begin{spacing}{1.1}
\begin{tabular}{l p{3cm} p{4.8cm} p{4.5cm}}
\toprule
\textbf{Layer} & \textbf{Component} & \textbf{Main responsibility} & \textbf{Out of scope} \\
\midrule

Client layer
& \texttt{llm-app} CLI/GUI
& Discover tools, plan usage, open sessions, submit requests, handle approvals
& Direct tool access, direct kernel communication \\

Gateway layer
& \texttt{mcpd}
& Load manifests, compute semantic hash, validate payloads, bind sessions, route requests, communicate with kernel
& Durable approval and audit state that must survive daemon restart \\

Kernel layer
& \texttt{kernel\_\allowbreak mcp}
& Maintain registries, perform admission decisions, manage approval tickets, verify session bindings, pin \texttt{binary\_\allowbreak hash}, expose \texttt{sysfs} state
& JSON parsing, tool execution, LLM-specific logic \\

Execution layer
& Tool services
& Execute application logic and return structured results
& Any control-plane role (admission, session binding, approval tracking) \\

\bottomrule
\end{tabular}
\end{spacing}
\caption{Concrete layers in \texttt{linux-mcp}.}
\label{tab:architecture_layers}
\end{table}



\subsection{Control-Plane Workflow}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/linux-mcp-workload.pdf}
\caption{Invocation lifecycle in \texttt{linux-mcp}.}
\label{fig:chapter3-lifecycle}
\end{figure}

The end-to-end path of a single invocation is:
\[
\texttt{llm-app} \to \texttt{mcpd} \to \texttt{kernel\_\allowbreak mcp} \to
\texttt{mcpd} \to \texttt{tool-app} \to \texttt{mcpd} \to \texttt{llm-app}
\]
Figure~\ref{fig:chapter3-lifecycle} illustrates this path as a sequence diagram.
This path captures one \texttt{tool:exec} invocation; a one-time \texttt{open\_\allowbreak session} handshake precedes the first such invocation on a given session and is described below.
A request originates in \texttt{llm-app}, which first issues a catalog query that \texttt{mcpd} satisfies from its manifest-derived in-memory state.
The kernel occupies this path not as a passive observer but as the active arbitration point: \texttt{mcpd} cannot forward a request to a tool without first obtaining an \texttt{ALLOW} decision from the kernel, and it cannot close an invocation without reporting \texttt{TOOL\_\allowbreak COMPLETE} back through the same channel.
Admission and completion are explicit, kernel-visible state transitions; the daemon no longer makes these decisions in private.

Before issuing any \texttt{tool:exec}, the client opens a session with an \texttt{open\_\allowbreak session} call.
On receiving this, \texttt{mcpd} reads the peer's credentials from the UDS connection via \texttt{SO\_\allowbreak PEERCRED}, hashes $(\text{uid}, \text{gid}, \text{pid}, \text{session\_id})$ into a binding hash, and registers the binding with the kernel.
This binding is the anchor the kernel re-verifies on every subsequent request from the session.

The client uses the catalog response to construct a structured invocation payload and submits it as a \texttt{tool:exec} call to \texttt{mcpd}; the complete message schema is given in Appendix~A.1.

Upon receiving the request, \texttt{mcpd} performs a series of user-space checks: it resolves the active session, validates the payload against the declared input schema, identifies the target tool, and retrieves the semantic hash associated with that tool from its manifest-derived state.
Only upon successful completion of these checks does \texttt{mcpd} issue a \texttt{TOOL\_\allowbreak REQUEST} to the kernel, supplying the agent identifier, tool identifier, semantic hash, the session binding hash (derived from \texttt{SO\_\allowbreak PEERCRED} at \texttt{open\_\allowbreak session}), and the live-probed \texttt{binary\_\allowbreak hash} of the backend process as parameters.

The kernel module then performs arbitration.
It verifies that the requesting agent appears in the agent registry, that the target tool appears in the tool registry, that the semantic hash in the request matches the hash recorded at registration time, that the session binding hash in the request matches the one registered at \texttt{open\_\allowbreak session}, and that the \texttt{binary\_\allowbreak hash} (TOFU-pinned on the first non-empty value) still matches on this invocation.
Based on these checks and the tool's declared policy, the kernel returns one of three decisions: \texttt{ALLOW}, in which case the request may proceed immediately and the kernel issues an approval ticket that the subsequent \texttt{TOOL\_\allowbreak COMPLETE} will reference; \texttt{DEFER}, in which case the request is held pending an explicit \texttt{APPROVAL\_\allowbreak DECIDE}; or \texttt{DENY}, in which case the request is rejected outright.
The detailed semantics of these decisions are described in Figure~\ref{fig:chapter3-approval}.

Depending on the kernel's decision, \texttt{mcpd} either forwards the request to the tool service, transitions the request into a pending state awaiting explicit approval, or terminates the invocation with an error.
After the tool service returns (with a normal result, an error, a timeout, or a crash caught by \texttt{mcpd}), \texttt{mcpd} issues \texttt{TOOL\_\allowbreak COMPLETE} to the kernel, referencing the ticket issued at admission; the kernel records the outcome in the per-agent \texttt{call\_\allowbreak log} and closes the invocation lifecycle from the control plane's perspective.

This workflow has a concrete observability consequence: the kernel both opens and closes every invocation.
An invocation that never reaches \texttt{TOOL\_\allowbreak COMPLETE}, whether due to daemon failure or tool timeout, persists as an open entry in kernel state and remains inspectable via \texttt{sysfs}.
This contrasts with a purely user-space design, in which the lifecycle of a request is only as durable as the daemon's own in-memory representation.



\subsection{Wire Protocols}
\label{sec:wire-protocols}

\texttt{linux-mcp} uses two transport families: Unix domain sockets for user-space communication, and Generic Netlink for the kernel control-plane path.
The user-space UDS variant is operator-configurable: filesystem-path UDS (\texttt{uds\_rpc}) is the default; abstract-namespace UDS (\texttt{uds\_abstract}) is also implemented and exercised end-to-end by one demo manifest; a VSOCK variant (\texttt{vsock\_rpc}) is reserved but not yet wired up.
Regardless of variant, the UDS paths between \texttt{llm-app} and \texttt{mcpd}, and between \texttt{mcpd} and each tool service, share a uniform framing: a 4-byte big-endian length prefix followed by a UTF-8-encoded JSON object.
A single framing routine therefore suffices on both user-space endpoints.
The Netlink path uses TLV-encoded attributes, not JSON; it is described at the end of this subsection.

\paragraph{llm-app to mcpd.}
\texttt{llm-app} sends two categories of message to \texttt{mcpd}: system control messages keyed by \texttt{"sys"} (covering \texttt{list\_\allowbreak apps}, \texttt{list\_\allowbreak tools}, \texttt{open\_\allowbreak session}, and \texttt{approval\_\allowbreak reply}) and tool execution requests keyed by \texttt{"kind": "tool:exec"}.
The two categories are distinguished by the top-level JSON key alone; \texttt{tool:exec} dominates in typical workloads, with \texttt{sys} messages appearing only at session boundaries.
On deferral, the \texttt{tool:exec} response carries a \texttt{ticket\_\allowbreak id} that the client submits with a subsequent \texttt{approval\_\allowbreak reply} to unblock the request.
Complete JSON schemas for all five messages are given in Appendix~A.1.

\paragraph{mcpd to tool-app.}
Once the kernel returns \texttt{ALLOW}, \texttt{mcpd} forwards the request to the relevant tool service over a per-application UDS connection using a five-field request / five-field response schema.
Consistent with the design goal in Section~\ref{sec:design-goals} that tool authors should not need to reason about the control plane, the tool service sees only a \texttt{(req\_id, agent\_id, tool\_id, operation, payload)} request and returns \texttt{(req\_id, status, result, error, t\_ms)}; it is unaware of sessions, kernel tickets, or the Netlink protocol.
The \texttt{operation} field is the dispatch string a tool service routes on and is declared per-tool in the manifest; it is distinct from the \texttt{tool\_id} (numeric, used by the kernel for registry indexing) and from the tool's human-readable \texttt{name}.
The full field specification is given in Appendix~A.1 (Table~\ref{tab:mcpd-toolapp}).

\paragraph{mcpd to kernel\_mcp (Generic Netlink).}
The kernel control-plane interface uses the \texttt{KERNEL\_\allowbreak MCP} Generic Netlink family.
Messages carry TLV-encoded attributes, and each message type is registered as a Netlink \emph{command}; all commands except the read-only \texttt{LIST\_\allowbreak TOOLS} dump require \texttt{CAP\_\allowbreak NET\_\allowbreak ADMIN}.
The command set covering registration, invocation, approval, and registry observability is listed in Table~\ref{tab:netlink-cmds}.
The attributes carrying the invariants the kernel actually checks on the critical \texttt{TOOL\_\allowbreak REQUEST}/\texttt{TOOL\_\allowbreak DECISION} path are shown in Table~\ref{tab:netlink-attrs}; the full attribute set and a reference Python framing routine are given in Appendix~A.1 (Table~\ref{tab:netlink-attrs-full}).

\begin{table}[H]
\centering\footnotesize
\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}lc p{8.2cm}@{}}
\toprule
\textbf{Command} & \textbf{\#} & \textbf{Purpose} \\
\midrule
\texttt{TOOL\_\allowbreak REGISTER}   & 3  & Register a tool with its semantic hash and risk flags \\
\texttt{AGENT\_\allowbreak REGISTER}  & 9  & Register an agent with its PID, UID, and binding credentials \\
\texttt{TOOL\_\allowbreak REQUEST}    & 10 & Request admission; kernel replies with \texttt{TOOL\_\allowbreak DECISION} \\
\texttt{TOOL\_\allowbreak DECISION}   & 11 & Kernel response: \texttt{ALLOW}\,(1), \texttt{DENY}\,(2), \texttt{DEFER}\,(3) \\
\texttt{TOOL\_\allowbreak COMPLETE}   & 12 & Close the invocation; records execution status and latency \\
\texttt{APPROVAL\_\allowbreak DECIDE} & 13 & Submit a user approval decision against an open ticket \\
\texttt{RESET\_\allowbreak TOOLS}     & 14 & Clear the kernel-side tool registry (issued on daemon restart) \\
\texttt{LIST\_\allowbreak TOOLS}      & 8  & Dump all registered tools (\texttt{NLM\_\allowbreak F\_\allowbreak DUMP} mode) \\
\bottomrule
\end{tabular}
\caption{\texttt{KERNEL\_\allowbreak MCP} Generic Netlink command set. All commands except \texttt{LIST\_\allowbreak TOOLS} require \texttt{CAP\_\allowbreak NET\_\allowbreak ADMIN}.}
\label{tab:netlink-cmds}
\end{table}

\vspace{-0.6em}
\begin{table}[H]
\centering\footnotesize
\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}lcl p{5.8cm}@{}}
\toprule
\textbf{Attribute} & \textbf{\#} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{TOOL\_HASH}             & 19 & \textit{string} & 64-char hex SHA-256 of manifest fields; drives the Substitution check \\
\texttt{BINARY\_\allowbreak HASH}       & 33 & \textit{string} & SHA-256 of backend's \texttt{/proc/<pid>/exe} (composite with script bytes for interpreter-hosted tools); drives the TOFU check \\
\texttt{AGENT\_\allowbreak BINDING}     & 28 & \textit{u64}    & Session binding hash over \texttt{SO\_\allowbreak PEERCRED}; drives the Spoofing check \\
\texttt{AGENT\_\allowbreak EPOCH}       & 29 & \textit{u64}    & Session opened-at epoch; drives the cross-session replay check \\
\texttt{DECISION}               & 17 & \textit{u32}    & Kernel verdict: 1=ALLOW, 2=DENY, 3=DEFER \\
\texttt{TICKET\_ID}             & 22 & \textit{u64}    & Approval ticket handle returned on DEFER and replayed on consume \\
\bottomrule
\end{tabular}
\caption{The six \texttt{KERNEL\_\allowbreak MCP} attributes on the admission-decision path, each tied to one defence in the Background threat model (Table~\ref{tab:threat_in_scope}). Routing-level attributes (\texttt{REQ\_ID}, \texttt{TOOL\_ID}, \texttt{AGENT\_ID}, \texttt{APPROVAL\_\allowbreak DECISION}) and the per-tool catalog epoch are documented in Appendix~A.1.}
\label{tab:netlink-attrs}
\end{table}

The \texttt{BINARY\_\allowbreak HASH} attribute warrants specific mention because its production is hybrid.
\texttt{mcpd} computes the digest in user space: it opens a socket to the backend, resolves the peer PID via \texttt{SO\_\allowbreak PEERCRED}, and hashes the bytes of \texttt{/\allowbreak proc/\allowbreak <pid>/\allowbreak exe} at probe time.
For interpreter-hosted tools whose manifest declares an entrypoint script, the digest is a composite of the interpreter executable and the on-disk script bytes, re-read on every probe, so substituting the script between invocations invalidates the pin.
The kernel applies trust-on-first-use enforcement to whatever \texttt{BINARY\_\allowbreak HASH} arrives: the first non-empty value (whether on \texttt{TOOL\_\allowbreak REGISTER} or a subsequent \texttt{TOOL\_\allowbreak REQUEST}) locks the slot, moving its \texttt{binary\_\allowbreak hash\_\allowbreak state} from \texttt{unpinned} to \texttt{live\_\allowbreak pinned}; any later \texttt{TOOL\_\allowbreak REQUEST} whose \texttt{BINARY\_\allowbreak HASH} fails to match the pinned value is denied with reason \texttt{BINARY\_\allowbreak MISMATCH}.
The kernel never reads \texttt{/proc} itself; its role is to store and compare, not to probe.

\paragraph{Tool registration via manifests.}
Tool services do not self-register; each application is described by a JSON manifest in \texttt{tool-app/\allowbreak manifests/} from which \texttt{mcpd} derives semantic hashes and issues \texttt{TOOL\_\allowbreak REGISTER} Netlink commands at startup.
The separation of the semantic hash (what the tool is) from the binding fingerprint over \texttt{endpoint} and \texttt{transport} (where the backend is reached), and the rebind-on-address-change mechanism it drives, are detailed in Section~\ref{sec:manifest-semantics}.
The complete field specification and a representative manifest are given in Appendix~A.1 (Table~\ref{tab:manifest-fields}).


\subsection{Manifest Semantics and Tool Identity}
\label{sec:manifest-semantics}

Defining tool identity at the right granularity is a delicate balance.
Binding identity to transport details such as socket paths or process identifiers makes routine operational changes, including redeployments, restarts, and reconfiguration, invalidate registry state even when the tool itself has not changed. 
If identity is defined too coarsely, however, the system can no longer distinguish a semantically modified tool from one that has merely been redeployed at a different address.

\texttt{linux-mcp} resolves this tension by separating semantic identity from transport location.
Each tool is described by a JSON manifest whose logical attributes are: tool identifier, tool name, application identifier, application name, risk tags, description, input schema, examples, path semantics, approval policy, and operation; these attributes describe what the tool is intended to do, not where it runs.
The \texttt{operation} field is the RPC method actually invoked on the backend, so a silent edit retargets execution to a different code path and must change the tool's identity.
\texttt{mcpd} canonicalises these fields into a stable JSON serialisation and computes its SHA-256 digest, represented as a 64-character hexadecimal string.
The full digest is used without truncation: a 32-bit prefix would reach a 50\% collision probability at roughly $2^{16}$ registrations by the birthday bound, and meaningful collision risk appears far earlier (even a few thousand tools put the collision probability well above acceptable thresholds for a primitive the kernel treats as a unique identity).

Two fields are excluded from the semantic hash by design: the \texttt{endpoint} and the \texttt{transport}.
Both describe \emph{where} a backend is reached, not \emph{what} it does, and routine operational moves (switching a service to a new socket path, or between transport variants such as \texttt{uds\_rpc} and \texttt{uds\_abstract}) should not cause a tool to look semantically different to planners or to kernel-side hash checks.
Instead, \texttt{mcpd} tracks these two fields in a separate \emph{binding fingerprint}, also a SHA-256 digest, maintained alongside but distinct from the semantic hash.
A change in either field produces a new binding fingerprint, which drives an unregister-then-re-register cycle against the kernel; the cycle clears the kernel's trust-on-first-use \texttt{binary\_\allowbreak hash} slot on the grounds that the backend reached at the new address may not be the same process.
The semantic hash is unaffected by the move, so planners and approval records that reference the tool remain valid.

Any modification to a semantically significant field, including the input schema, risk tags, description, approval policy, path semantics, or \texttt{operation}, produces a different semantic digest, which the kernel detects as a mismatch on the next \texttt{TOOL\_\allowbreak REQUEST}.
The semantic hash is the artefact that both sides check: computed in user space from the manifest, recorded in and verified by the kernel.



\subsection{Session Management and Approval Binding}

While deferring and resolving a request is mechanically straightforward, binding an approval to the exact session, requester, and operation that triggered it requires explicit ticket-level state and precise matching logic to prevent TOCTOU violations.

Each session is bound to the real peer credentials of its Unix domain socket connection~\cite{stevens2013unix}.
The daemon takes \texttt{uid}, \texttt{gid}, \texttt{pid}, and a per-session random \texttt{session\_\allowbreak id}, feeds them into BLAKE2b with an 8-byte output size, and stores the resulting 64-bit digest as the binding hash together with a session epoch.
Including \texttt{pid} and a random \texttt{session\_\allowbreak id} means two processes sharing a UID cannot reuse each other's session.
The 64-bit output matches the width of the \texttt{AGENT\_\allowbreak BINDING} Netlink attribute, so \texttt{mcpd} re-submits the hash on every \texttt{TOOL\_\allowbreak REQUEST} and the kernel compares it against the value registered at \texttt{open\_\allowbreak session}.
A \texttt{DEFER} response creates an approval ticket tagged with this binding context and a five-minute expiry, overridable per request.
When \texttt{APPROVAL\_\allowbreak DECIDE} arrives, the kernel re-checks the binding before letting the request proceed.

Session-scoped binding plus per-request re-verification rules out approval replay across sessions and keeps the state validated at deferral identical to the state at execution, so no TOCTOU window opens~\cite{bishop2003computer, lee2021exprace}.

\paragraph{Catalog-epoch rebind on manifest change.}
A second form of staleness arises when the catalog changes while a session is open.
Without further mechanism, a client could keep invoking a re-registered tool under its pre-change binding.
The check is scoped per tool.
Each tool records a \texttt{registered\_\allowbreak at\_\allowbreak epoch} at (re-)registration, each session records an \texttt{opened\_\allowbreak at\_\allowbreak epoch} at open time, and a \texttt{TOOL\_\allowbreak REQUEST} is denied with \texttt{catalog\_\allowbreak stale\_\allowbreak rebind\_\allowbreak required} when the targeted tool's \texttt{registered\_\allowbreak at\_\allowbreak epoch} exceeds the session's \texttt{opened\_\allowbreak at\_\allowbreak epoch}.
Only the affected tool's re-registration invalidates pending sessions.
Editing an unrelated manifest advances the shared \texttt{tool\_\allowbreak catalog\_\allowbreak epoch} counter without breaking other sessions.

Beyond the kernel-side ticket flow, \texttt{linux-mcp} supports two independent approval mechanisms driven by different manifest fields and enforced at different layers.
The kernel remains the sole arbiter on the control-plane path.
The user-space mechanism is a client-side pre-check that either terminates a request before it reaches \texttt{mcpd} or lets it proceed unchanged.

\textbf{(1) Kernel-mediated approval (DEFER ticket flow).}
The kernel decides between \texttt{ALLOW}, \texttt{DENY}, and \texttt{DEFER} on every \texttt{TOOL\_\allowbreak REQUEST} based on the tool's \texttt{risk\_\allowbreak flags}, a bitmask derived from the manifest's \texttt{risk\_\allowbreak tags} at registration time.
Tools tagged \texttt{FILESYSTEM\_\allowbreak DELETE}, \texttt{DEVICE\_\allowbreak CONTROL}, \texttt{EXTERNAL\_\allowbreak NETWORK}, \texttt{PRIVILEGED}, or \texttt{IRREVERSIBLE} are deferred by default.
The five validity checks that distinguish \texttt{ALLOW} from \texttt{DENY}, and the state machine that governs the resulting tickets, are given in full in the Kernel Module Design section.
When a request is deferred, \texttt{mcpd} records the pending operation and returns the ticket identifier to the client.
After the user approves, \texttt{mcpd} first sends \texttt{APPROVAL\_\allowbreak DECIDE} to the kernel to transition the ticket, then replays the original request with the ticket identifier.
The kernel consumes the ticket during the second \texttt{TOOL\_\allowbreak REQUEST} and returns \texttt{ALLOW}.

\textbf{(2) Client-side local approval (payload-aware prompt).}
\texttt{llm-app} consults each tool's \texttt{approval\_\allowbreak policy} field before sending \texttt{tool:exec} to \texttt{mcpd}.
Most tools omit the field and incur no local prompt.
A tool may declare \texttt{approval\_\allowbreak policy.user\_\allowbreak confirmation} with a condition such as \texttt{when\,=\,"path\_\allowbreak outside\_\allowbreak repo"}, in which case the client resolves the payload's declared path fields against the repository root and prompts the user if any of them resolve outside.
The demo tool \texttt{write\_\allowbreak host\_\allowbreak text\_\allowbreak file} in \texttt{04\_\allowbreak desktop\_\allowbreak app.json} uses exactly this to require confirmation before writing to the host filesystem outside the project directory.
A declined local prompt terminates the request in user space.
No \texttt{tool:exec} is sent to \texttt{mcpd} and no kernel ticket is created.

\textbf{Relationship between the two mechanisms.}
They are complementary, not mutually exclusive.
Kernel \texttt{DEFER} is \emph{payload-agnostic}; once a tool carries a high-risk flag, every invocation is gated regardless of arguments.
The local prompt is \emph{payload-aware} and only gates requests whose specific arguments meet the declared condition.
A single tool can trigger both, in which case execution proceeds only after the local prompt is approved, an \texttt{APPROVAL\_\allowbreak DECIDE} succeeds, and the ticket-bearing second \texttt{TOOL\_\allowbreak REQUEST} is admitted.

Figure~\ref{fig:chapter3-approval} illustrates the kernel-side state machine.
Approval resolution always happens \emph{before} tool execution, so no deferred request bypasses the approval gate and every deferred execution consumes an explicit ticket.
The kernel need not understand the full meaning of a request.
It verifies that the binding context is preserved, the ticket is valid and unconsumed, and the \texttt{DEFER} $\rightarrow$ \texttt{ALLOW} transition is explicitly authorised~\cite{klein2010sel4}.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/linux-mcp-approval.pdf}
\caption{Approval-ticket flow in \texttt{linux-mcp}: the \texttt{ALLOW}/\texttt{DENY}/\texttt{DEFER} state machine enforced by the kernel module.}
\label{fig:chapter3-approval}
\end{figure}



\subsection{Kernel Module Design}

\texttt{kernel\_\allowbreak mcp} is implemented as a loadable kernel module exposing the \texttt{KERNEL\_\allowbreak MCP} Generic Netlink family.
The command set covers registration, invocation, approval, and registry observability: \texttt{TOOL\_\allowbreak REGISTER}, \texttt{AGENT\_\allowbreak REGISTER}, \texttt{TOOL\_\allowbreak REQUEST}, \texttt{TOOL\_\allowbreak DECISION}, \texttt{TOOL\_\allowbreak COMPLETE}, \texttt{APPROVAL\_\allowbreak DECIDE}, \texttt{RESET\_\allowbreak TOOLS}, and \texttt{LIST\_\allowbreak TOOLS}.
Generic Netlink was chosen over \texttt{ioctl} or a character device because it provides structured TLV attributes with kernel-side schema validation, native support for dump-style enumeration (used by \texttt{LIST\_\allowbreak TOOLS}), and per-command \texttt{GENL\_\allowbreak ADMIN\_\allowbreak PERM} gating that integrates cleanly with Linux capabilities~\cite{netlink2026}.

Internally, the module maintains two registries.
The tool registry maps tool identifiers to their manifest-derived semantic hashes and associated metadata.
Each entry also carries risk flags, a per-tool \texttt{registered\_\allowbreak at\_\allowbreak epoch}, and the TOFU \texttt{binary\_\allowbreak hash} slot.
The agent registry records agents permitted to submit requests through the control plane, together with the \texttt{binding\_\allowbreak hash}, \texttt{binding\_\allowbreak epoch}, and \texttt{opened\_\allowbreak at\_\allowbreak epoch} captured at \texttt{open\_\allowbreak session}.
Approval tickets are heap-allocated kernel objects with an explicit state machine.
The normal path runs \texttt{PENDING} $\to$ \texttt{APPROVED} $\to$ \texttt{CONSUMED}.
A reject takes \texttt{PENDING} to \texttt{DENIED}.
A post-approval revoke takes \texttt{APPROVED} to \texttt{DENIED}.
\texttt{EXPIRED} is implicit; an expired ticket is removed from the table, not marked with a stored state.

The arbitration logic is narrow by design.
A \texttt{TOOL\_\allowbreak REQUEST} is admitted only if five conditions all hold.
The submitting agent must appear in the agent registry.
The target tool must appear in the tool registry.
The semantic hash supplied by \texttt{mcpd} must match the hash recorded at registration.
The session \texttt{binding\_\allowbreak hash} in the request must match the one registered at \texttt{open\_\allowbreak session}.
If the TOFU slot is already pinned, the \texttt{binary\_\allowbreak hash} in the request must match the pinned value.
Any failing check produces \texttt{DENY} with a specific reason code.
Once the checks pass, the kernel consults the tool's \texttt{risk\_\allowbreak flags}.
A tool carrying \texttt{FILESYSTEM\_\allowbreak DELETE}, \texttt{DEVICE\_\allowbreak CONTROL}, \texttt{EXTERNAL\_\allowbreak NETWORK}, \texttt{PRIVILEGED}, or \texttt{IRREVERSIBLE} is deferred and receives a fresh approval ticket; everything else gets \texttt{ALLOW}.

\paragraph{Trust-on-first-use backend binary pinning.}
Semantic-hash admission guards what a tool claims to be, not which binary actually serves the socket.
The kernel therefore keeps a per-tool \texttt{binary\_\allowbreak hash} slot that \texttt{mcpd} fills on a live probe.
\texttt{mcpd} reads the backend's peer PID via \texttt{SO\_\allowbreak PEERCRED} and hashes \texttt{/\allowbreak proc/\allowbreak <pid>/\allowbreak exe}.
For interpreter-hosted tools the digest combines the interpreter with the on-disk script, so a script swap is still caught.
The slot locks on the first non-empty digest, which usually arrives on \texttt{TOOL\_\allowbreak REGISTER} but may arrive on the first successful \texttt{TOOL\_\allowbreak REQUEST} if the backend comes up later.
Any subsequent \texttt{TOOL\_\allowbreak REQUEST} whose digest does not match is rejected with reason \texttt{BINARY\_\allowbreak MISMATCH}.
A \texttt{binary\_\allowbreak hash\_\allowbreak state} field distinguishes \texttt{unpinned} from \texttt{live\_\allowbreak pinned}, since both previously read as an empty string.

\paragraph{Kernel-resident call log.}
Each agent carries a fixed-size 32-slot circular buffer of invocation records.
A record is a flat struct holding a monotonic sequence number, a nanosecond timestamp, the request and tool ids, a status (one of \texttt{OK}, \texttt{ERR}, \texttt{DENY}, \texttt{DEFER}), execution latency, a tool status code similar to a process exit code, 8-byte payload and response hash prefixes, and up to 48 leading bytes of any error output.
Completed invocations log from \texttt{cmd\_\allowbreak tool\_\allowbreak complete}; denied or deferred ones log from \texttt{cmd\_\allowbreak tool\_\allowbreak request}, so nothing escapes the audit buffer.
The kernel parses no JSON.
\texttt{mcpd} precomputes the hashes and passes them as fixed-width attributes.
The buffer is exposed through \texttt{sysfs} as a binary attribute and stays readable after a \texttt{mcpd} crash.
This provides crash-visible audit, not tamper-evident provenance: the buffer is bounded and the hashes are 8-byte prefixes.

\paragraph{Sysfs surface.}
Each tool directory under \texttt{/\allowbreak sys/\allowbreak kernel/\allowbreak mcp/\allowbreak tools/} exposes \texttt{name}, semantic \texttt{hash}, \texttt{binary\_\allowbreak hash}, \texttt{binary\_\allowbreak hash\_\allowbreak state}, and \texttt{registered\_\allowbreak at\_\allowbreak epoch}.
The global \texttt{tool\_\allowbreak catalog\_\allowbreak epoch} sits at the top of the tree.
Per-agent entries under \texttt{/\allowbreak sys/\allowbreak kernel/\allowbreak mcp/\allowbreak agents/} expose per-state counters (\texttt{allow\_\allowbreak count}, \texttt{deny\_\allowbreak count}, \texttt{defer\_\allowbreak count}, \texttt{completed\_\allowbreak ok\_\allowbreak count}, \texttt{completed\_\allowbreak err\_\allowbreak count}, \texttt{last\_\allowbreak exec\_\allowbreak ms}), a human-readable \texttt{last\_\allowbreak reason}, and the \texttt{call\_\allowbreak log} above with its head and count indices.
An operator can read what the kernel regards as authoritative without relying on daemon-side logs.



\subsection{Daemon Implementation}
\label{sec:daemon-implementation}

The kernel enforces the critical state transitions, but \texttt{mcpd} does most of the actual work.
Its code splits across four concerns.
\texttt{manifest\_\allowbreak loader.py} handles manifest loading and semantic hashing.
\texttt{session\_\allowbreak store.py} manages session storage and binding context.
\texttt{netlink\_\allowbreak client.py} carries Netlink traffic to the kernel.
\texttt{server.py} dispatches requests and runs the backend probe.
Sections~\ref{sec:system-architecture}, \ref{sec:manifest-semantics}, \ref{sec:wire-protocols}, and the session discussion above cover each area in detail.
Two daemon-specific behaviours deserve separate treatment below.
One is how \texttt{mcpd} keeps the kernel registry consistent across startups and manifest edits.
The other is how it closes the TOFU \texttt{binary\_\allowbreak hash} loop when the backend is unreachable.

\paragraph{Kernel state reconciliation.}
The manifest directory is the source of truth, but kernel registry state persists across \texttt{mcpd} restarts.
A cold start or a manifest edit must neither drift silently from the on-disk manifests nor invalidate existing sessions unnecessarily.
On every reload, the daemon computes a per-tool diff between the previous and new registries and collapses it into one of five reconcile actions.
\texttt{KEEP} applies when both fingerprints match and makes no kernel call.
\texttt{ADD} and \texttt{REMOVE} handle tools that appear or disappear between reloads.
\texttt{RE\_\allowbreak REGISTER\_\allowbreak SEMANTIC} covers a changed semantic hash on an unchanged backend, and the in-place re-register preserves the kernel-side TOFU \texttt{binary\_\allowbreak hash} slot.
\texttt{RE\_\allowbreak REGISTER\_\allowbreak BINDING\_\allowbreak MOVE} fires when \texttt{transport} or \texttt{endpoint} changed, which means the backend may now be a different process.
The daemon first unregisters to clear the TOFU slot, then re-registers so the next probe pins a fresh digest.
Two fingerprints from Section~\ref{sec:manifest-semantics} drive the decision.
A change in \texttt{manifest\_\allowbreak hash} alone selects \texttt{RE\_\allowbreak REGISTER\_\allowbreak SEMANTIC}; a change in \texttt{binding\_\allowbreak fingerprint} escalates to \texttt{RE\_\allowbreak REGISTER\_\allowbreak BINDING\_\allowbreak MOVE}.

\paragraph{Probe-failure fail-closed.}
The TOFU \texttt{binary\_\allowbreak hash} mechanism only works if the daemon never quietly substitutes an empty digest for a failed probe.
If \texttt{mcpd} cannot reach the backend, cannot read \texttt{/\allowbreak proc/\allowbreak <pid>/\allowbreak exe}, or does not recognise the transport, the probe returns a non-live result and the exec path refuses the invocation.
No empty \texttt{BINARY\_\allowbreak HASH} flows upward.
This closes two silent failure modes.
A fresh probe that cannot read the live binary will not reset a pinned slot and let a swapped backend start clean.
A cold registration before the backend comes up will not install a blanket empty pin that a later swap could inherit.
Audit records for probe refusals carry \texttt{source=probe}, which separates them from kernel-side denials (\texttt{source=arb}) and client-side declines (\texttt{source=user\_\allowbreak decline}).

\paragraph{Netlink client.}
\texttt{mcpd} keeps a single long-lived Netlink socket to \texttt{kernel\_\allowbreak mcp} instead of reopening one per request.
Socket setup adds measurable overhead on an otherwise microsecond-scale path, and a persistent connection simplifies the ordering of \texttt{APPROVAL\_\allowbreak DECIDE} against its eventual \texttt{TOOL\_\allowbreak REQUEST} replay.
Optional per-stage latency counters (session lookup, kernel arbitration, tool execution, end-to-end) are available on this path for the overhead analysis in Chapter~\ref{chap:evaluation}.

\paragraph{Deployment and fail-closed configuration.}
\texttt{mcpd} needs \texttt{CAP\_\allowbreak NET\_\allowbreak ADMIN} for Netlink administration and \texttt{CAP\_\allowbreak SYS\_\allowbreak PTRACE} for reading \texttt{/\allowbreak proc/\allowbreak <pid>/\allowbreak exe} across uid boundaries during the backend probe.
The supplied systemd unit runs the daemon as a dedicated unprivileged \texttt{mcpd} user and grants both capabilities via \texttt{Ambient\allowbreak Capabilities}.
The same two capabilities also appear in \texttt{Capability\allowbreak Bounding\allowbreak Set}, so an \texttt{execve}-launched child cannot regain anything beyond what \texttt{mcpd} itself uses.
\texttt{Protect\allowbreak System=strict} with a \texttt{Read\allowbreak Write\allowbreak Paths} allowlist of \texttt{/\allowbreak tmp} and \texttt{/\allowbreak tmp/\allowbreak linux-mcp-apps} renders the rest of the filesystem, including the manifest directory, read-only to the daemon, with no explicit bind mount required.

Two further directives harden the process itself.
\texttt{No\allowbreak New\allowbreak Privileges=true} blocks any child \texttt{execve} from picking up capabilities \texttt{mcpd} lacks.
\texttt{Memory\allowbreak Deny\allowbreak Write\allowbreak Execute=true} prevents a memory-corruption exploit from mapping writable executable pages, which blunts an attack class the threat model otherwise excludes.
Several standard directives shrink the blast radius if \texttt{mcpd} is compromised.
\texttt{Protect\allowbreak Kernel\allowbreak Tunables}, \texttt{Protect\allowbreak Kernel\allowbreak Modules}, and \texttt{Protect\allowbreak Control\allowbreak Groups} block kernel-state probing.
\texttt{Restrict\allowbreak SUIDSGID} and \texttt{Lock\allowbreak Personality} cut off SUID and personality-flag tricks.
\texttt{Private\allowbreak Devices} strips the \texttt{/dev} access \texttt{mcpd} never uses.

A separate question is which UIDs the daemon accepts as tool backends.
\texttt{mcpd} checks \texttt{allowed\_\allowbreak backend\_\allowbreak uids} via \texttt{SO\_\allowbreak PEERCRED} on every probe and exec dial.
An unprivileged \texttt{mcpd} defaults to its own UID.
A privileged \texttt{mcpd} has no safe default.
Trusting only UID~0 rejects every realistic non-root backend and leaves \texttt{binary\_\allowbreak hash} unpinned.
Trusting \texttt{\$SUDO\_\allowbreak UID} folds the correctness of the launcher script into the daemon's trust boundary.
\texttt{mcpd} therefore refuses to start unless the list is declared in the TOML, or the operator explicitly opts in via \texttt{LINUX\_\allowbreak MCP\_\allowbreak TRUST\_\allowbreak SUDO\_\allowbreak UID}.
The supplied systemd unit sets \texttt{LINUX\_\allowbreak MCP\_\allowbreak CONFIG} to a TOML path and leaves \texttt{LINUX\_\allowbreak MCP\_\allowbreak TRUST\_\allowbreak SUDO\_\allowbreak UID} unset.
Production deployments therefore name backend UIDs in configuration; they do not inherit them from the launch environment.



\subsection{State Reconciliation and Recovery}
\label{sec:reconciliation}

\texttt{linux-mcp} treats a daemon restart as a recovery problem, not a process-management detail.
The three layers of state it keeps sit on different sides of the crash boundary.
The kernel holds the tool registry, approval tickets, the \texttt{tool\_\allowbreak catalog\_\allowbreak epoch} counter, and each agent's \texttt{call\_\allowbreak log}.
All of that survives a \texttt{mcpd} crash.
The manifest set on disk is the authoritative source of tool identity and is unaffected by restart.
Open sessions, peer-credential bindings, and pending request maps live only in \texttt{mcpd}'s memory and are discarded when the process exits.

The cold-start procedure follows directly from this partition.
At startup, \texttt{mcpd} issues \texttt{RESET\_\allowbreak TOOLS} to clear the kernel tool registry and then re-registers each manifest tool unconditionally, which guarantees the kernel and the on-disk manifest set agree before any client is accepted.
\texttt{RESET\_\allowbreak TOOLS} alone does not bump the catalog epoch; the per-tool register calls that follow do.
A surviving session holding a pre-restart \texttt{opened\_\allowbreak at\_\allowbreak epoch} will therefore receive \texttt{catalog\_\allowbreak stale\_\allowbreak rebind\_\allowbreak required} on its next request against any re-registered tool.
The per-tool diff-based reconciliation described in Section~\ref{sec:daemon-implementation} applies to later manifest edits, not to this cold-start path.

This split preserves audit and admission state without pretending that a crashed daemon can safely continue pre-existing sessions.
Approval tickets and registry history stay inspectable through \texttt{sysfs} across the restart, so an operator can still read what recent activity occurred while \texttt{mcpd} was down or unresponsive.
Any surviving ticket whose referenced session no longer has a live \texttt{mcpd}-side binding simply expires at its TTL.
Clients reopen any session that pre-dates the restart on their first post-restart request.
The client library handles this rebind automatically when it sees the \texttt{catalog\_\allowbreak stale\_\allowbreak rebind\_\allowbreak required} reason.

A kernel module reload sits outside this recovery model.
Unloading \texttt{kernel\_\allowbreak mcp} drops all kernel-held state (registry, tickets, epochs, call logs), so the operator must treat the next start as a fresh system.
\texttt{mcpd} re-registers from manifests as usual, but any historical audit kept only in the now-gone kernel buffer is lost.



\subsection{Rationale for the Kernel--User-Space Split}
\label{sec:design-alternatives}

Three kernel-interface designs were considered before settling on Generic Netlink.

The first candidate was eBPF.
It looked lightweight, needed no full kernel module, and is increasingly the standard way to extend kernel behaviour.
An early design review exposed two obstacles rooted in eBPF LSM's hook-attachment model.
eBPF LSM hooks attach to kernel operations such as \texttt{security\_\allowbreak socket\_\allowbreak recvmsg} and \texttt{security\_\allowbreak file\_\allowbreak open}, not to parsed JSON-RPC messages.
An in-kernel enforcer would therefore re-parse MCP framing from raw socket bytes on every hook invocation before it could see a \texttt{tool\_\allowbreak hash} or a \texttt{ticket\_\allowbreak id}.
That parsing also has to fit under the eBPF verifier's bounded-loop and instruction-count constraints while handling variable-length strings and nested JSON objects.
State management itself is tractable in eBPF; \texttt{bpf\_\allowbreak timer} and pinned maps are adequate for approval tickets and expiry.
Reaching that state, however, means rebuilding the application-layer protocol state machine inside verifier-constrained programs.
That cost is what made eBPF unattractive for this particular control plane.

LSM hooks were the second candidate.
They fire at the syscall boundary, which is the wrong layer.
Tool identity in this system is a SHA-256 hash of a JSON manifest, not a file path or a socket operation.
No syscall naturally sees a tool hash alongside a request; correlating the two would inject application-layer semantics into a mechanism designed for OS-object-level policy.
The mismatch is a layer-level one, not a matter of implementation effort.

A capability-based design in the style of Capsicum was also considered.
It would have required tool services to interact with a kernel-facing API to obtain and present capabilities, which breaks the goal that tool authors need only write a JSON manifest with no kernel-facing obligations.

Generic Netlink avoids all three problems.
It supports structured TLV messages, dump-style enumeration for registry introspection, and arbitrary internal state management in a loadable module.
The cost is that a full kernel module is needed where a policy hook or a BPF program would have sufficed for simpler designs.
For a prototype targeting a narrow set of admission invariants this is an acceptable trade-off, and the module's role stayed scoped enough that the added complexity remained manageable.



\subsection{Simplifications and Optimisation Opportunities}

The prototype is scoped for control-plane correctness, not peak throughput.
Several choices along the way combine genuine scalability limits with deliberate simplifications, and each is worth naming so it is neither mistaken for a fundamental limit nor inherited uncritically.

A single global lock protects the kernel registries.
This is adequate at the evaluated catalog sizes but becomes a scalability concern under higher concurrency.
Replacing it (for example with RCU-protected reads on the request path) is the most mechanical of the items listed here.

Cold-start reconciliation is unconditional.
Every launch pays a \texttt{RESET\_\allowbreak TOOLS} plus a full \texttt{TOOL\_\allowbreak REGISTER} sweep, even when manifests are unchanged.
This is correctness-first, as discussed in Section~\ref{sec:reconciliation}; the daemon has no trustworthy way to prove that its in-memory view matches the kernel's without the rebuild.
An optimisation that skips the sweep when a manifest-set signature is unchanged would cut startup cost at the price of more elaborate startup logic.

Hot reload is request-driven, not filesystem-driven.
\texttt{mcpd} recomputes a manifest-set signature on the \texttt{list\_\allowbreak apps}, \texttt{list\_\allowbreak tools}, and \texttt{tool:exec} paths and refreshes the kernel registry when the signature changes, so a manifest edit takes effect on the next request, not the instant the file is written.
The request-driven variant avoids an \texttt{inotify} dependency and the race between a partially-written manifest and a concurrent request, at the cost of a bounded staleness window between edit and uptake.

The kernel performs no per-agent rate limiting; throttling is left to \texttt{mcpd}.
This keeps the kernel module scope narrow (no policy language, no token buckets) and concentrates policy decisions in the layer that already tracks session context.
One consequence is that the kernel cannot defend against a compromised \texttt{mcpd} issuing a flood of well-formed \texttt{TOOL\_\allowbreak REQUEST}s, a limitation consistent with the time-partitioned trust model of Section~\ref{sec:threat-model}.

Section~\ref{sec:further-work} discusses resolution directions for each.
