% !TEX root = ../main.tex
\section*{Chapter 4: Results and Discussion}
\setcounter{section}{4} \setcounter{subsection}{0}
\addcontentsline{toc}{section}{\textit{Chapter 4: Results and Discussion}}
\phantomsection \label{chap:evaluation}

% Discourage float-only pages: require ≥85% text on a regular page,
% and demand ≥90% fill before a page is promoted to a float-only page.
\renewcommand{\topfraction}{0.85}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.15}
\renewcommand{\floatpagefraction}{0.9}

linux-mcp is evaluated along four dimensions: control-plane latency and its breakdown across invocation stages, throughput under concurrent load, resistance to a representative set of control-plane attacks, and system behaviour under daemon failure and restart.
The central claim under test is that relocating admission-control state into the kernel strengthens enforcement without making the tool invocation path prohibitively expensive.
Two baselines isolate this effect from user-space hardening alone: a conventional user-space mediator (\textbf{user-space}), in which registration, admission, and approval all reside in the daemon, and a hardened variant (\textbf{seccomp}) that augments the same control path with stricter syscall filtering, additional validation, and audit logging.

The prototype under test comprises 43 tools across 14 applications; all three systems operate over this same tool deployment throughout the evaluation.
All experiments were conducted on a VMware guest (Apple aarch64 host, 4 vCPU, 8 GB RAM) in a headless configuration with no concurrent user-space workloads.
Latency results are reported over $n = 30$ independent runs of 2000 requests each, giving a standard error of the mean small enough to support Welch's $t$-test comparisons at $\alpha = 0.05$ while remaining within the hypervisor timing noise of a virtualised guest.
Throughput and the category-level security evaluation are drawn from the same run, using the harness's built-in concurrency sweep and ten repeats of each attack case.
All reported $p$-values are two-sided Welch's $t$-tests \cite{west2021welch} comparing per-repetition mean latency against the \texttt{user-space} baseline.


\subsection{Latency and Control-Plane Overhead}

\begin{table}[!b]
\centering
\caption{Mean end-to-end latency by payload ($n = 30$ runs $\times$ 2000 requests). $\Delta$ columns report the relative change against the \texttt{user-space} baseline on the same row; $p$-values are two-sided Welch's $t$-tests against that baseline, \textsuperscript{ns} $= p > 0.05$. Bold marks the row minimum.}
\label{tab:latency}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.2}
\footnotesize
\begin{tabular}{l r @{\hspace{12pt}} r r l @{\hspace{12pt}} r r l}
\toprule
& \multicolumn{1}{c}{\textbf{user-space}}
& \multicolumn{3}{c}{\textbf{seccomp}}
& \multicolumn{3}{c}{\textbf{kernel}} \\
\cmidrule(lr){2-2}\cmidrule(lr){3-5}\cmidrule(lr){6-8}
\textbf{Payload}
  & \textbf{ms}
  & \textbf{ms} & \textbf{$\Delta$} & \textbf{$p$}
  & \textbf{ms} & \textbf{$\Delta$} & \textbf{$p$} \\
\midrule
\rowcolor{gray!8}
100\,B & \textbf{0.854} & 0.867          & $+1.5\%$  & $0.48^{\text{ns}}$ & \textbf{0.854} & $\ \ 0.0\%$ & $0.98^{\text{ns}}$ \\
10\,KiB & \textbf{0.863} & 0.922          & $+6.8\%$  & $2.1{\times}10^{-4}$  & 0.899          & $+4.2\%$    & $0.10^{\text{ns}}$ \\
\rowcolor{gray!8}
1\,MiB  & 7.748          & 9.843          & $+27.0\%$ & $<10^{-4}$            & \textbf{7.487} & $-3.4\%$    & $2.7{\times}10^{-4}$ \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:latency} and panel~(a) of Figure~\ref{fig:chapter4-latency} report mean end-to-end latency for each system at three payload sizes.
The 100~B and 10~KiB points correspond to typical MCP tool invocations (short JSON commands and small structured responses); the 1~MiB point is a contrived stress case, used solely to probe how the control plane behaves when the payload dominates the cost of a request, and is not intended to reflect everyday tool traffic.
At 100~B, the kernel path and the user-space baseline are within measurement noise: both at \num{0.854}~ms ($p = 0.984$).
At 10~KiB the kernel path is nominally slower (\num{0.899}~ms versus \num{0.863}~ms, +4.2\%), but the difference does not reach significance ($p = 0.097$), a sub-millisecond effect lost in the timing noise of a VMware guest at $n = 30$.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth]{figures/figure_latency_panel.pdf}
\caption{Latency summary across the three systems at 100~B, 10~KiB, and 1~MiB payloads ($n = 30$ independent runs of 2000 requests, error bars 95\% CI). Panel (a): mean end-to-end latency on a log y-axis so sub-millisecond gaps at small payloads remain visible alongside the 1~MiB values. Panel (b): $p_{95}$ latency; the three systems are indistinguishable at 100~B and 10~KiB, and the kernel $p_{95}$ sits below both baselines at 1~MiB. Panel (c): mean-latency ratio against the \texttt{user-space} baseline, zoomed around the parity line; \texttt{seccomp} sits above parity at every payload size, while the kernel path only crosses below parity at 1~MiB. Panel (d): per-payload stage stack in $\mu$s, with three sub-axes auto-scaled per payload; each bar decomposes one system into session-lookup, arbitration, and tool-exec. The annotation above each kernel bar reports the absolute kernel arbitration cost (\num{28}~$\mu$s at 100~B and 10~KiB, \num{38}~$\mu$s at 1~MiB), which is the only stage with a meaningful inter-system delta and remains roughly one order of magnitude below tool execution at small payloads (tool exec / arbitration $\approx$ 10$\times$) and at least two orders of magnitude below at 1~MiB ($\approx$ 102$\times$ against tool execution alone, and $\approx$ 197$\times$ against full end-to-end latency).}
\label{fig:chapter4-latency}
\end{figure}

The 1~MiB row provides the only statistically clean comparison in the table, with the kernel path at \num{7.487}~ms against \num{7.748}~ms for plain user-space (a 3.4\% reduction, $p = 0.00027$) and \num{9.843}~ms for the hardened variant.
This is counter-intuitive: adding a privilege boundary is usually expected to cost latency, not return it.
Panel~(c) of Figure~\ref{fig:chapter4-latency} shows the same data as a ratio against the plain \texttt{user-space} baseline, with the y-axis zoomed around the parity line so that the sub-4\% effects at 100~B and 10~KiB are visible in the same frame as the larger effect at 1~MiB; read that way, \texttt{seccomp} sits above parity at every payload size while the kernel path crosses below parity only at 1~MiB.
The seccomp penalty at large payloads has a clean explanation: BPF per-syscall filtering pays a constant cost on every \texttt{sendmsg}/\texttt{recvmsg} crossing, and a megabyte payload forces many more such crossings through the tool-forwarding socket than the smaller payloads, a cost the kernel path does not incur.
The 3.4\% speedup of the kernel path over plain user-space at 1~MiB is harder to attribute confidently from the available stage-level instrumentation: both paths admitted these requests (DENY would produce a different latency distribution) and both subsequently copied the same 1~MiB payload through the same \texttt{mcpd} $\to$ tool-app UDS, so an ``avoided copy'' explanation does not hold.
Plausible contributors include scheduler effects (\texttt{mcpd} releases the CPU while waiting on the kernel Netlink reply, which may interact favourably with how the guest kernel schedules a 1~MiB \texttt{sendmsg}) and hypervisor-level timing jitter (a 3.4\% effect at the 7--8~ms scale falls within the noise envelope a VMware guest typically exhibits at this latency range); the present instrumentation does not separate them.
We report the effect because it is statistically clean ($p = 2.7 \times 10^{-4}$ at $n = 30$) but treat it as evidence that the kernel path does not impose a measurable overhead at this payload size, not as evidence of a positive speedup mechanism.

The $p_{95}$ picture, shown in panel~(b) of Figure~\ref{fig:chapter4-latency}, matches the means for the kernel path: at both 100~B and 10~KiB the kernel $p_{95}$ is statistically indistinguishable from user-space ($p = 0.51$ and $p = 0.16$).
The seccomp $p_{95}$ already separates from user-space at 10~KiB (\num{1.078}~ms vs \num{1.003}~ms; $p = 6.4 \times 10^{-3}$), so the seccomp tail is the first to widen under load even though the means are still close.
At 1~MiB the kernel $p_{95}$ is \num{8.422}~ms, against \num{8.716}~ms for user-space ($p = 0.049$) and \num{10.903}~ms for seccomp ($p < 10^{-4}$).
The kernel tail does not widen relative to user-space across any of the measured payloads.

The stage-level breakdown in panel~(d) of Figure~\ref{fig:chapter4-latency} confirms that kernel arbitration is a minor contributor at all payload sizes.
Mean arbitration time is \num{0.028}~ms at 100~B and 10~KiB, and \num{0.038}~ms at 1~MiB.
At 1~MiB, tool execution accounts for \num{89.3}\% of the instrumented stage total (session lookup + arbitration + tool execution = \num{4.351}~ms on the kernel path) and \num{89.9}\% of the same total on the user-space path; arbitration contributes less than four hundredths of a millisecond within that bucket.
Measured against the full end-to-end mean latency (\num{7.487}~ms on kernel, \num{7.748}~ms on user-space), tool execution itself is closer to half (\num{51.9}\% and \num{49.9}\% respectively) and the difference is unaccounted-for cost outside the instrumented stages (chiefly the \texttt{mcpd} $\to$ tool-app UDS round-trip, JSON framing, and process scheduling), which scales with payload size and dominates the absolute number at 1~MiB.

\begin{figure}[!t]
\centering
\includegraphics[width=0.95\linewidth]{figures/figure_latency_ecdf.pdf}
\caption{Empirical CDF of per-run mean latency ($n = 30$ runs per payload per system), with each subplot auto-scaled in x so the relevant regime is visible. Overlapping curves at 100~B and 10~KiB indicate that the per-run means are indistinguishable across systems; at 1~MiB the \texttt{seccomp} curve shifts rightward and the \texttt{kernel} curve lies to the left of \texttt{user-space}, with no heavy right tail on the kernel path.}
\label{fig:chapter4-latency-ecdf}
\end{figure}

Figure~\ref{fig:chapter4-latency-ecdf} complements the summary statistics with the empirical distribution of per-run mean latency at each payload size.
The ECDF view shows the same three regimes as distributions instead of point estimates: at 100~B and 10~KiB the three curves overlap almost completely, indicating that the per-run means fall into the same distribution; at 1~MiB the \texttt{seccomp} curve shifts rightward by roughly 2~ms while the \texttt{kernel} curve lies to the left of \texttt{user-space}.
No heavy right tail appears on the kernel path at any payload size, consistent with the $p_{95}$ result.


A supplementary microbenchmark isolates the Generic Netlink layer using a minimum-cost \texttt{KMCP\_\allowbreak CMD\_\allowbreak NOOP} handler added to the kernel module.
The NOOP path returns immediately after Generic Netlink demultiplexing, without touching any tool or agent registry, and therefore measures the irreducible floor of a round-trip against which all per-stage arbitration costs can be anchored.
Figure~\ref{fig:chapter4-ablation} reports the medians on a single axis: the NOOP floor sits at \num{3.125}~$\mu$s, the full \texttt{TOOL\_\allowbreak REQUEST} path measured at a realistic registry scale of 1024 tools and 64 agents sits at \num{7.459}~$\mu$s, and the \texttt{skip\_\allowbreak lookups} and \texttt{ticket\_\allowbreak trigger} bars bracket the per-stage contributions that the next paragraph decomposes.
The total kernel arbitration cost is therefore \num{4.334}~$\mu$s above the noise floor, against the \num{28}--\num{38}~$\mu$s figure that the end-to-end stage breakdown attributes to ``kernel arbitration'' in panel~(d). The order-of-magnitude gap reflects what each measurement covers: the microbenchmark times only the kernel-side handler, while the end-to-end breakdown also captures the user-space\,$\to$\,kernel\,$\to$\,user-space Generic Netlink round-trip, the Python framing inside \texttt{mcpd}, and the socket I/O for both the request and the reply. The dominant cost on the kernel path therefore comes from user-space components, not from the kernel module itself.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{figures/figure_ablation_mean_rtt.pdf}
\caption{Kernel path ablation at realistic registry scale ($N = 1024$ tools, $M = 64$ agents). Bar heights report median RTT ($p_{50}$) over $n = 10$ repetitions ($\geq$\,1000 requests per cell, $\geq$\,2000 for the NOOP floor); error bars are omitted because the CI95 column in the source data describes the mean rather than the median. The leftmost bar is the measured noise floor from the \texttt{KMCP\_\allowbreak CMD\_\allowbreak NOOP} handler, which establishes the irreducible Generic Netlink round-trip cost. The \texttt{full} bar is the complete \texttt{TOOL\_\allowbreak REQUEST} arbitration path; \texttt{skip\_\allowbreak lookups} disables the tool and agent registry walks; the two \texttt{ticket\_\allowbreak trigger} bars exercise a high-risk tool whose execution is gated by the approval-ticket machinery, with and without the approval branch bypassed; \texttt{skip\_\allowbreak hash} and \texttt{skip\_\allowbreak binding} bypass the hash and binding-mismatch guard bodies (dead code under benign workload). The dotted line and inline annotation visualise the +\num{4.334}~$\mu$s arbitration delta over the NOOP floor that paragraph above quantifies.}
\label{fig:chapter4-ablation}
\end{figure}

To decompose the \num{4.334}~$\mu$s arbitration cost into its constituent stages, a paired ablation was performed against compile-time experiment flags that selectively bypass individual kernel code paths (\texttt{SKIP\_\allowbreak LOOKUPS}, \texttt{SKIP\_HASH}, \texttt{SKIP\_\allowbreak BINDING}, \texttt{SKIP\_\allowbreak TICKET}).
Each stage delta is measured by alternating the baseline and bypass modes request-by-request within a single benchmark phase, so the two samples in every pair are collected microseconds apart and share the same scheduler state and cache residency.
Hypervisor-level drift that contaminates naive sequential-mean comparisons cancels out inside the paired difference, and bootstrap CIs on the delta vector are correspondingly tighter.

Table~\ref{tab:ablation-stages} and Figure~\ref{fig:chapter4-ablation-stages} together report the two stages whose guard bodies are actually executed by the benchmark workload, plus two methodology-check rows that document the path-identity caveat.
The \texttt{registry+agent\_\allowbreak lookup} stage (two mutex pairs, an \texttt{xa\_load} against a 1024-entry xarray, and a 64-entry hashtable walk) contributes \num{0.109}~$\mu$s per request.
The \texttt{approval\_\allowbreak ticket\_\allowbreak body} stage (consume plus issue, including an \texttt{approval\_\allowbreak lock} acquisition and hashtable insert) contributes \num{2.012}~$\mu$s per request and is only exercised when the tool is high-risk and the request arrives without a pre-authorised ticket.
Under benign credentials the \texttt{hash} and \texttt{binding} guard bodies are dead code (short-circuited by the workload state before the experiment flag is consulted), and their paired deltas correctly return $0 \pm$ noise with $p_{\text{BH}} > 0.5$, confirming that the paired method introduces no systematic bias on identical code paths.

\begin{table}[!htbp]
\centering
\caption{Paired per-stage ablation deltas. Each row alternates a baseline mode and a bypass mode request-by-request inside a single phase; the $\Delta$ column is the mean of $\text{baseline}_i - \text{bypass}_i$ across all pairs. BH-corrected paired $t$-tests are reported across all four comparisons.}
\label{tab:ablation-stages}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lrrlrr}
\toprule
\textbf{Stage} & $n_{\text{pairs}}$ & $\Delta_{\text{avg}}$ ($\mu$s) & \textbf{95\% CI} ($\mu$s) & $t_{\text{paired}}$ & $p_{\text{BH}}$ \\
\midrule
registry+agent\_lookup & 100\,000 & $+0.109$ & $[+0.087, +0.131]$ & $+10.08$ & $<10^{-4}$ \\
approval\_ticket\_body   & 1\,000   & $+2.012$ & $[+1.760, +2.257]$ & $+15.99$ & $<10^{-4}$ \\
\midrule
\multicolumn{6}{l}{\textit{Methodology consistency checks (dead guard bodies under benign workload)}} \\
hash\_guard\_body        & 100\,000 & $+0.163$ & $[-0.153, +0.541]$ & $+0.88$  & $0.507$ \\
binding\_guard\_body     & 100\,000 & $\sim0.000$ & $[-0.027, +0.013]$ & $\sim0.00$  & $0.513$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{figures/figure_ablation_stage_delta.pdf}
\caption{Paired per-stage deltas across the four ablation comparisons. The first two bars are the meaningful stage costs (registry+agent lookup and the approval-ticket body); the last two are methodology consistency checks against dead guard bodies and are expected to fall on zero.}
\label{fig:chapter4-ablation-stages}
\end{figure}

Subtracting the \num{0.109}~$\mu$s registry+lookup cost from the \num{4.334}~$\mu$s total leaves roughly \num{4.2}~$\mu$s unattributed in the benign run (the approval-ticket body is not exercised along this path, so its cost does not enter the sum).
The remainder is reply-skb construction: the five \texttt{nla\_put\_*} calls and the \texttt{genlmsg\_\allowbreak reply} in \texttt{kernel\_\allowbreak mcp\_\allowbreak reply\_\allowbreak tool\_\allowbreak decision}, which the experiment flags do not decompose further.

One caveat about the \texttt{approval\_\allowbreak ticket\_\allowbreak body} figure deserves explicit mention.
Every ticket issuance calls \texttt{kernel\_\allowbreak mcp\_\allowbreak purge\_\allowbreak expired\_\allowbreak tickets\_\allowbreak locked()}, which iterates the full 256-bucket approval hashtable.
Under \num{300}~s TTL, no ticket expires during a one-minute microbenchmark, so the purge scan cost grows linearly with the number of tickets the benchmark has already issued.
Moving the expiry scan to the existing periodic \texttt{kernel\_\allowbreak mcp\_\allowbreak ticket\_\allowbreak cleanup\_\allowbreak timer} would eliminate this $O(n)$ component entirely.
This is flagged as a simple future-work optimisation, not a characteristic of the fast path in production use where \texttt{mcpd} consumes tickets promptly and the hashtable stays small.


\subsection{Throughput and Scalability}

Throughput was measured across five agent counts (1, 5, 10, 20, and 50) and four concurrency levels (1, 10, 50, and 100) to check whether the kernel-assisted control plane becomes a bottleneck once multiple requests are in flight simultaneously.
Table~\ref{tab:throughput} gives the per-cell peak throughput at one agent; the complete sweep across agent counts produces the same shape and is summarised in prose below.

\begin{table}[!htbp]
\centering
\caption{Peak throughput at one agent, across concurrency levels ($n = 5$ repetitions per cell). Ratio columns report throughput relative to \texttt{user-space} on the same row. The kernel path tracks the user-space baseline within $\sim$10\% at every concurrency level and does not collapse inside the tested window.}
\label{tab:throughput}
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.2}
\footnotesize
\begin{tabular}{r r @{\hspace{10pt}} r r @{\hspace{10pt}} r r}
\toprule
& \multicolumn{1}{c}{\textbf{user-space}}
& \multicolumn{2}{c}{\textbf{seccomp}}
& \multicolumn{2}{c}{\textbf{kernel}} \\
\cmidrule(lr){2-2}\cmidrule(lr){3-4}\cmidrule(lr){5-6}
\textbf{Conc.}
  & \textbf{RPS}
  & \textbf{RPS} & \textbf{rel.}
  & \textbf{RPS} & \textbf{rel.} \\
\midrule
\rowcolor{gray!8}
1   & 1117.5 & 1088.5 & $0.974\times$ & 1071.1 & $0.958\times$ \\
10  & 1146.0 & 1036.9 & $0.905\times$ & 1009.4 & $0.881\times$ \\
\rowcolor{gray!8}
50  & 1215.6 & 1123.5 & $0.924\times$ & 1096.2 & $0.902\times$ \\
100 & 1213.4 & 1072.5 & $0.884\times$ & 1103.7 & $0.910\times$ \\
\bottomrule
\end{tabular}
\end{table}

Plain user-space achieves the highest peak throughput across the full agent$\times$concurrency sweep (\num{1090}--\num{1219}~RPS, plotted in Figure~\ref{fig:chapter4-throughput-sweep}).
Over the same 5$\times$4 grid of agent counts and concurrency levels, the kernel path ranges from \num{1003} to \num{1137}~RPS and the seccomp path from \num{1020} to \num{1200}~RPS; Table~\ref{tab:throughput} reports only the agents=1 row, so its kernel column (\num{1009}--\num{1104}~RPS) is a tighter slice of this wider range.
At a representative operating point of one agent and concurrency 50, the three systems sit at \num{1215.6}, \num{1123.5}, and \num{1096.2}~RPS respectively.
The kernel path is roughly 10\% slower than the user-space peak, which is consistent with the additional Generic Netlink round-trip and with the serialisation of admission decisions through a single kernel module.
Figure~\ref{fig:chapter4-throughput-sweep} plots the complete agent$\times$concurrency grid and shows that the relative ordering is stable across all five agent counts: the \texttt{user-space} line sits at the top, the \texttt{seccomp} and \texttt{kernel} lines track each other within a few percent, and none of the three curves bends downward as concurrency climbs from 1 to 100.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.98\linewidth]{figures/figure_throughput_sweep.pdf}
\caption{Throughput (RPS) across the full agent$\times$concurrency sweep ($n = 5$ repetitions per cell, error bars 95\% CI). Each panel fixes an agent count and varies concurrency on a log axis. The relative ordering is stable across all five panels, and none of the three systems collapses inside the tested window.}
\label{fig:chapter4-throughput-sweep}
\end{figure}

These numbers come from an earlier $n = 5$ pilot run.
The $n = 30$ study that drives the latency table was scoped to latency alone and did not repeat the concurrency sweep, so the throughput figures carry wider uncertainty than the latency figures and should be read as evidence of practical viability, not as a precise ceiling.
Within the tested range the kernel path stays within roughly 10\% of the user-space peak without collapsing, which is enough to support the viability claim.
A follow-up sustained-overload run was attempted at concurrencies of 200, 400, and 800, but the \texttt{SOMAXCONN} accept queue saturated before the control plane itself became the bottleneck, so those cells produced error-dominated latency distributions instead of usable p99 curves and are not reported here.


\subsection{Registry Scaling}
\label{sec:registry-scaling}

A separate experiment characterises how the kernel-side tool registry behaves as the catalogue grows beyond the 43-tool deployment used elsewhere in the evaluation.
$N$ synthetic tools (with $N \in \{8, 16, 32, \ldots, 16\,384\}$) are bulk-registered into the kernel, and both the register path and the steady-state lookup path are measured.
The register path is modelled as a one-time setup cost plus a per-tool steady-state cost:
\[
\text{total\_ms}(N) = a + b \cdot N,
\]
fitted across the 12 values of $N$ by ordinary least squares.

The fit produces $a = -0.123$~ms and $b = 6.010$~$\mu$s per tool, with $R^2 = 0.9996$ on the linear total-time fit.
That $R^2$ is close to 1 mostly because total\_ms varies over three orders of magnitude with $N$ and so reflects the dominance of the linear term, not the goodness of the per-tool view; a more direct test of the linear-scaling hypothesis is the log-log regression $\log(\text{total\_ms}) = \alpha + \beta \log N$, which gives $\beta = 0.986$ with a bootstrap 95\% CI of $[0.958, 1.014]$ that contains $1.0$. This supports the claim that total registration cost scales linearly with $N$ across the measured range, independent of the per-tool view.
The asymptotic registration throughput is therefore $1/b \approx 166\,000$ tools/s; the 43-tool catalogue used in the main evaluation registers in approximately \num{0.14}~ms of wall-clock time (extrapolated from the linear fit), and even a \num{16384}-tool deployment completes its one-shot registration in under \num{100}~ms.
The per-tool cost (Figure~\ref{fig:chapter4-registry-curve}) sits within roughly $\pm 1$~$\mu$s of the asymptote across three orders of magnitude of $N$, and is indistinguishable from a constant for $N \geq 2^7$. The apparent excursion at small $N$ is the intercept $a$ amortising away rather than a scaling degradation.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{figures/figure_registry_register_curve.pdf}
\caption{Registry register-path per-tool cost as a function of registry size $N$, with $95\%$ CI error bars. The dashed line is the asymptotic per-tool cost $b = 6.010$~$\mu$s, taken as the slope of the linear fit $\text{total\_ms}(N) = a + b N$ used in Section~\ref{sec:registry-scaling}. We do not overlay the per-tool fit curve $a/N + b$: the OLS-derived intercept $a$ is slightly negative (an artefact of the fit being dominated by large-$N$ residuals in the millisecond range), so the curve would reach the asymptote from below while the data reaches it from above. Measured per-tool cost stays within roughly $\pm 1$~$\mu$s of the asymptote across three orders of magnitude of $N$ and is indistinguishable from a constant for $N \geq 2^7$.}
\label{fig:chapter4-registry-curve}
\end{figure}

The steady-state lookup path is measured separately by drawing a random \texttt{tool\_id} for each request against the pre-populated registry; the log-linear regression $\text{lookup\_ms} \sim a + b \cdot \log_2 N$ produces a slope of \num{2.9e-5}~ms per doubling with a bootstrap 95\% CI of $[-2\times 10^{-6}, +6.1\times 10^{-5}]$ that contains zero ($R^2 = 0.26$).
The near-zero slope and the CI overlapping zero are consistent with the expected $O(1)$ behaviour of the underlying xarray data structure, and inconsistent with any balanced-tree implementation where slope would grow with $\log_2 N$ at high $R^2$.
Figure~\ref{fig:chapter4-registry-lookup} shows the lookup latency distribution across the full range of $N$.
Mean lookup latency stays within the 7.1--7.7~$\mu$s band across three orders of magnitude of registry size.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{figures/figure_registry_lookup_scaling.pdf}
\caption{Steady-state \texttt{tool\_id} lookup latency as a function of registry size $N$, on a log-$N$ axis. The fitted log-linear slope is $2.9\times 10^{-5}$~ms per doubling (bootstrap 95\% CI $[-2\times 10^{-6}, +6.1\times 10^{-5}]$, containing zero), consistent with the $O(1)$ xarray lookup invariant.}
\label{fig:chapter4-registry-lookup}
\end{figure}


\subsection{Security Evaluation}

The latency and throughput numbers establish that kernel placement is not prohibitively expensive, but the argument for making the move hinges on whether it actually changes what attackers can do.
The security evaluation targets four categories from the threat model in \hyperref[chap:background]{Chapter~2}: identity spoofing (forging session identity to impersonate a registered agent), replay (reusing a stale approval ticket), substitution (tampering with \texttt{tool\_id} or \texttt{tool\_hash} to redirect execution), and escalation (bypassing the approval gate on a high-risk invocation).

Each system received 130 attack attempts in total (13 distinct cases $\times$ 10 repeats: 3 spoof, 5 replay, 3 substitution, 2 escalation).
Outcomes are summarised in Table~\ref{tab:attacks} and Figure~\ref{fig:chapter4-security}.
Before reading the headline numbers, one methodology note bears emphasis: the user-space rates reported here reflect the per-category instrumented weakening described later in this section; they are \emph{not} the performance of a fully hardened user-space implementation.

\begin{table}[H]
\centering
\caption{Category-level attack outcomes by system (130 attempts each: 3 spoof $+$ 5 replay $+$ 3 substitution $+$ 2 escalation, $\times\,10$ repeats). ``Bypasses'' counts attempts that circumvented the intended control. 95\% Clopper--Pearson confidence intervals on the bypass rate are reported in the last column. The 84.6\% user-space rate is \emph{not} a uniform per-case bypass: 11 of the 13 case rows are admitted in full ($10/10$ each $= 110$), while two cases (\texttt{spoof / session\_token\_theft} and \texttt{replay / cross\_tool\_ticket\_reuse}) are blocked by orthogonal in-process checks even on the unhardened user-space mediator and contribute 0 admissions to the headline. The full per-case decomposition is plotted in Figure~\ref{fig:chapter4-security}.}
\label{tab:attacks}
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.25}
\footnotesize
\begin{tabular}{l r r r l}
\toprule
\textbf{System} & \textbf{Bypasses} & \textbf{Attempts} & \textbf{Bypass rate} & \textbf{95\% CI} \\
\midrule
\rowcolor{red!10}
\texttt{user-space} & 110 & 130 & $84.6\%$ & $[77.2,\,90.3]$ \\
\rowcolor{orange!10}
\texttt{seccomp}   &  50 & 130 & $38.5\%$ & $[30.1,\,47.4]$ \\
\rowcolor{green!10}
\texttt{kernel}    &   0 & 130 & $\mathbf{0.0\%}$ & $[0.0,\,2.8]$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.8\linewidth]{figures/figure_attack_heatmap.pdf}
\caption{Attack bypass outcome per case and system (13 per-case rows grouped by attack type, 10 repetitions per cell). Dark cells indicate the attempt was fully blocked; white cells with a $\mathrm{successes}/\mathrm{attempts}$ label indicate a full bypass; hatched white cells indicate a partial bypass. The \texttt{kernel} column is uniformly blocked across all 13 per-case rows, while the two user-space baselines permit bypasses on the specific checks that their instrumented tamper profiles relax.}
\label{fig:chapter4-security}
\end{figure}

The plain user-space baseline is the weakest of the three: the harness selectively weakens its code paths per category (\texttt{tamper\_\allowbreak session}, \texttt{tamper\_\allowbreak approval}, \texttt{tamper\_\allowbreak metadata}, \texttt{compromised\_\allowbreak userspace}) so that each test exercises the specific check it targets, and the resulting bypass rate in Table~\ref{tab:attacks} shows what a user-space mediator cannot structurally protect when its own state is under attack, not what a fully hardened version could achieve.
The seccomp variant tightens checks and narrows the syscall surface but keeps admission as a user-space decision, so attacks that do not need to cross a privilege boundary (particularly substitution and spoofing) still get through.

The kernel path blocks every attempt in the table.

Spoofing fails because agent identity is tied to a kernel-registered identifier that the control plane verifies independently of the daemon's session state.
Replay fails because approval tickets are kernel-resident lifecycle objects with explicit \texttt{consumed} and \texttt{expired} fields; once a ticket has been advanced or has passed its validity window, no subsequent \texttt{APPROVAL\_\allowbreak DECIDE} can reopen it.
Substitution is blocked by the semantic hash check: regardless of whether an attacker modifies the \texttt{tool\_hash} field in the request or replaces the tool endpoint at runtime, the kernel compares the submitted hash against the one recorded at registration and rejects any mismatch.
A supplementary injection experiment confirms this independently: in 30 live \texttt{llm-app} $\to$ \texttt{mcpd} $\to$ \texttt{kernel} execution chains, every runtime hash substitution produced a \texttt{DENY} with \texttt{reason=hash\_\allowbreak mismatch} at the kernel layer, regardless of which tool the LLM planner had selected.

One implementation detail of the ticket lifecycle strengthens the replay defence beyond what the category table directly measures: a ticket is bound to the session, agent, tool, and hash, and also to the exact \texttt{req\_id} of the issuing request.
A replay consume attempt that reuses a different \texttt{req\_id} returns \texttt{approval\_\allowbreak ticket\_\allowbreak scope\_\allowbreak mismatch}, making it substantially harder to exploit a stolen ticket identifier in a fresh request context.

Two scoping limits bound the interpretation of Table~\ref{tab:attacks}. Each attack category maps one-to-one onto an enforcement mechanism in the kernel module, so the 0\% bypass rate is a functional-correctness test of four specified invariants against inputs constructed to exercise exactly those invariants, not evidence of adversarial robustness against unknown attacks. The combined attack set is also scoped to the stated control-plane invariants and excludes attacks assuming compromise of the kernel module itself, attacks exploiting semantic vulnerabilities in tool logic, attacks requiring already-obtained privileged capabilities (such as \texttt{CAP\_\allowbreak NET\_\allowbreak ADMIN}), and attacks against the LLM prompt-construction pipeline. The two dominant threat classes in AI-agent deployments, prompt injection \cite{greshake2023prompt, liu2023prompt} and supply-chain compromise of manifests, live at layers where kernel admission control has no visibility (the planner's reasoning layer and the filesystem integrity layer respectively) and are addressed by complementary defences rather than by this work.



\subsection{Extended Threat Model}
\label{sec:extended-attacks}

The 130-attempt category-level evaluation in Table~\ref{tab:attacks} covers design-time invariants that the kernel module was built to enforce.
A separate experiment targets three threat classes that sit outside that set and that a design-time review would not surface: a time-of-check-time-of-use (TOCTOU) race on the approval window, a cross-UID session hijack under adversarial Generic Netlink credentials, and dumb fuzzing of the \texttt{KERNEL\_\allowbreak MCP} Generic Netlink family.

\paragraph{TOCTOU on the approval window.}
Two concurrent threads race against a high-risk tool: one repeatedly consumes approval tickets while the other re-registers the underlying tool binding, creating a window in which a ticket issued against one binding could in principle be consumed against another.
Across \num{10000} iterations, zero \texttt{ALLOW} outcomes were produced: \num{9985} requests were rejected with \texttt{hash\_\allowbreak mismatch} at the kernel boundary, and the remaining 15 were deferred with \texttt{approval\_\allowbreak ticket\_\allowbreak unknown} because the consume attempt raced ahead of ticket issuance.
The Clopper--Pearson 95\% CI on the blocked rate is $[0.9996, 1.0000]$.
The absence of breaches is consistent with the lock ordering in \texttt{kernel\_\allowbreak mcp\_\allowbreak cmd\_\allowbreak tool\_\allowbreak request}: the ticket consume and the tool-hash recomputation both acquire \texttt{tool\_lock}, so a replacement that swaps the underlying binding cannot land between the two checks.

\paragraph{Cross-UID session hijack.}
A session is opened by UID $A$, and a second process running as UID $B$ attempts three replay variants: blind replay, replay with a guessed session identifier, and replay with a leaked session identifier.
Across \num{500} attempts, this experiment surfaced a defect that a design-time review of the kernel module had missed.
In the version merged from the main evaluation branch, \texttt{kernel\_\allowbreak mcp\_\allowbreak cmd\_\allowbreak tool\_\allowbreak request} never compares \texttt{NETLINK\_\allowbreak CB(skb)} peer credentials against the \texttt{uid} recorded at agent registration, so a crafted-credential replay from UID $B$ reaches the request handler as if it had been sent by UID $A$.
Of the 500 attempts, 326 were still blocked by downstream \texttt{binding\_\allowbreak mismatch} at the kernel boundary against the replayed request body, but 174 passed every kernel-side check that the stock binary enforces and would have been admitted to tool execution.
Those 174 are what the zero-bypass numbers in Table~\ref{tab:attacks} do not cover, because the category-level harness never probes a second UID.

The fix is a six-line patch that walks \texttt{sock\_\allowbreak i\_\allowbreak uid(NETLINK\_\allowbreak CB(skb).sk)} on entry to the handler and rejects the request with \texttt{-EPERM} on mismatch, gated behind a new \texttt{require\_\allowbreak peer\_\allowbreak cred} module parameter so that the A/B comparison can be run against a single binary.
With the gate enabled, all \num{500} attempts are blocked at peer-credential verification before any registry work runs; the Clopper--Pearson 95\% CIs for the two conditions do not overlap.
The patch adds no measurable latency because the peer-credential read is on the same cache line as the existing \texttt{NETLINK\_\allowbreak CB} access that the handler already performs.
Figure~\ref{fig:chapter4-e4-peer-cred} visualises the A/B outcome: in the stock binary all 174 \texttt{leaked} attempts are admitted, while the same input set is uniformly blocked at \texttt{peer\_\allowbreak cred\_\allowbreak mismatch} once the gate is enabled.
This evaluation-discovered defect changes the interpretation of the zero-bypass result in Table~\ref{tab:attacks}: the category-level harness did not cover cross-UID replay, and the stock binary did not enforce the peer-credential invariant until the E4 experiment exposed the gap.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth]{figures/figure_e4_peer_cred_ab.pdf}
\caption{Cross-UID hijack outcomes by attack sub-case (\texttt{blind}, \texttt{guessed}, \texttt{leaked}) and binary state, $n = 500$ per panel. Panel (a) is the stock module: only the \texttt{leaked} sub-case (where UID $B$ has obtained UID $A$'s 64-bit binding-hash cookie) reaches the request handler unchallenged, admitting all 174 attempts to tool execution. Panel (b) is the same module rebuilt with \texttt{require\_\allowbreak peer\_\allowbreak cred = 1}: all 500 attempts are rejected at the new peer-credential check before the request reaches the registry walks. The DENY-path median latency is unchanged between the two conditions, so the additional check imposes no measurable runtime cost.}
\label{fig:chapter4-e4-peer-cred}
\end{figure}

\paragraph{Generic Netlink dumb fuzzer.}
The \texttt{KERNEL\_\allowbreak MCP} family was exercised with a coverage-free structured fuzzer that iterates every defined \texttt{KMCP\_\allowbreak CMD\_\allowbreak *} opcode and applies per-\texttt{nlattr} bit flipping, length mutations, and type substitutions.
Over a \num{30}-minute fuzzing window, \num{880640} mutated messages were delivered to the kernel module.
Monitoring the kernel log buffer across the run (\texttt{dmesg} delta plus \texttt{kmemleak} scan) produced zero \texttt{oops}, \texttt{WARN}, \texttt{BUG}, \texttt{GPF}, or memory-leak reports.
Every malformed input was rejected with an \texttt{errno} drawn from a bounded set of five expected classes (\texttt{-EPERM}, \texttt{-ENOENT}, \texttt{-EFAULT}, \texttt{-EINVAL}, \texttt{-ERANGE}); we do not claim per-input determinism (the harness did not replay individual inputs to verify same-input/same-errno behaviour), only that no input fell outside this set.
The fuzzer's design time was limited, so the result is a robustness smoke test, not evidence of adversarial hardness: it says the Generic Netlink dispatch layer does not crash on malformed input within the tested \num{30}-minute budget, not that the kernel module is free of latent memory-safety bugs under an unbounded adversary.


\subsection{Daemon Failure and Recovery}
\label{sec:daemon-failure}

A core architectural argument for kernel-held state is that it should outlive the process that registered it.
To test this property, the daemon was forcibly interrupted mid-operation and control-plane state was observed before and after restart.

In both user-space systems, daemon failure is control-plane failure.
Registered tool state, in-flight sessions, and pending approval tickets are all bound to the process that held them.
When it dies, they are gone.
Recovery means starting over or reloading from application-level logs, with no consistency guarantee.

The kernel path is different.
Approval tickets, the tool registry, and agent registrations survive the crash intact and remain inspectable via sysfs without any daemon running.
Session state is not preserved; the binding context lives in daemon memory and is inherently tied to an active connection.

The durability claim here is observability-oriented, not warm-continuation-oriented: while \texttt{mcpd} is down, sysfs stays readable so an operator can still inspect the surviving registry, ticket state, and per-agent \texttt{call\_\allowbreak log} entries.
On daemon restart, the cold-start path described in Section~\ref{sec:reconciliation} issues \texttt{RESET\_\allowbreak TOOLS} and re-registers each manifest tool unconditionally, so the surviving kernel state serves audit rather than warm continuation; the conservative rebuild avoids relying on a residual view whose freshness the daemon cannot prove.
Incremental reconciliation is already used while \texttt{mcpd} is running (only tools whose semantic or binding fingerprint changed are re-registered); extending that behaviour to the cold-start path, gated on a divergence signal, would reduce restart overhead and is a natural direction for future work.


\subsection{Discussion}

Seccomp does more work than plain user-space, including tighter syscall filters, extra validation, and audit logging, yet it still admits \num{38.5}\% of attacks (Table~\ref{tab:attacks}) and is the slowest system at megabyte payloads (Table~\ref{tab:latency}).
The kernel path blocks every attempt because the admission state sits where no unprivileged process can reach it; seccomp keeps filtering around the same mutable process that holds the decision.

The 1~MiB latency picture is the same architectural fact viewed from the performance side: kernel arbitration runs on a fixed-size Generic Netlink message regardless of payload, while seccomp's BPF filter pays per syscall and so accumulates cost across the heavier socket traffic a megabyte payload forces through.
The kernel path's 3.4\% latency reduction over plain user-space at 1~MiB (Table~\ref{tab:latency}) is statistically clean but not clearly attributable from the available stage instrumentation; we read it as evidence of no measurable overhead at this payload size, not as evidence of a positive speedup mechanism.
The ablation breakdown agrees: of the \num{4.334}~$\mu$s that kernel arbitration adds over the NOOP floor, registry+lookup costs \num{0.109}~$\mu$s and the rest is reply-skb construction.
Nothing in the fast path scales with tool count or request body, and the registry scaling experiment extends this end-to-end: lookup latency stays within a \num{0.6}~$\mu$s band from $N = 8$ to $N = 16\,384$, and registration runs at roughly \num{166000} tools per second.

Cross-UID (Section~\ref{sec:extended-attacks}) is the clearest reminder that the zero-bypass table measures only design-time invariants: it exposed a peer-credential gap the category-level harness never probed, closed by a six-line patch at no measurable latency cost; the design-time and extended-attack layers are complementary, and neither alone would have caught this.

Within the swept window of up to 100 concurrent requests the kernel path holds within roughly 10\% of plain user-space throughput; beyond it, the global registry lock is the obvious suspect and per-tool or per-agent lock decomposition the natural fix, but both lie outside the scope of this evaluation.



\subsection{Threats to Validity}

The results should be interpreted within four scope limitations.
First, all experiments ran in a single VMware guest on an aarch64 host, so latency figures are environment-specific and generalisation to bare-metal or different kernel configurations is not established.
Second, the evaluation uses demonstration tools with predictable execution times and does not characterise tail latency under sustained load.
Third, the attack study is a controlled functional-correctness test of the four explicitly identified invariants and does not constitute a robustness assessment against unknown attacks, prompt injection, or privileged adversaries.
Fourth, kernel-level admission control addresses only one layer of an AI-agent system, leaving semantic tool vulnerabilities, unsafe policy choices, and compromise outside the stated threat model unaddressed.
